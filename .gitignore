__pycache__/
*.pyc
*.egg-info/
.ipynb_checkpoints/

# 51 MB of rendered HTML/PDF reports from an earlier chapter -- not needed to
# reproduce anything here, and it is what makes a clone slow.
reports/

# Experiment output lives on /local1, not in the repo. results/all_runs.csv and
# results/manifests/ are the small, checked-in extract that lets someone audit the
# numbers without re-running 30 GPU-hours.
outputs/
*.jsonl
*.mp4
